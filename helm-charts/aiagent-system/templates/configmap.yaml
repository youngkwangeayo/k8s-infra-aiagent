apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aiagent-system.fullname" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "aiagent-system.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
data:
  config.js: |
{{ .Values.configJs | indent 4 }}
