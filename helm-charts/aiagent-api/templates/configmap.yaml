apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aiagent-api.fullname" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{ include "aiagent-api.labels" . | nindent 4 }}
data:
  config.json: |
{{ .Values.configJson | indent 4 }}
