apiVersion: v1
kind: ConfigMap
metadata:
  name: aiagent-system-config
  namespace: dev-aiagent
  labels:
    app.kubernetes.io/name: aiagent-system
    app.kubernetes.io/app: aiagent-system
    app.kubernetes.io/version: "1"
    app.kubernetes.io/component: frontend
data:
  config.js: |
    export default {
      "NODE_ENV":"local",
    };
