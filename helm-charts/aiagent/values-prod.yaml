replicaCount: 2

namespace: "prod-aiagent"

image:
  pullPolicy: IfNotPresent
  tag: "0.0.2"

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 6

configJs: |
    export default {
      "NODE_ENV":"prod"
    };
