replicaCount: 2

namespace: "dev-aiagent"

image:
  pullPolicy: IfNotPresent
  tag: "0.0.2"

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 6

configJs: |
    export default {
      "NODE_ENV":"local"
    };

