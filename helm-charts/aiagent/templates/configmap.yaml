apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aiagent.fullname" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "aiagent.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
data:
  config.js: |
{{ .Values.configJs | nindent 4 }}
